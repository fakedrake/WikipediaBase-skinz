============
Installation
============

At the command line::

    $ easy_install wikipediabase

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv wikipediabase
    $ pip install wikipediabase